//now let's add the color that we will need to project
import 'package:flutter/material.dart';

class MainColor {
  static Color primaryColor = Color(0xffa2a1a2);
  static Color secondaryColor = Color(0xFF7DD3CF);
  static Color accentColor = Color(0xffb39ebe);
}
